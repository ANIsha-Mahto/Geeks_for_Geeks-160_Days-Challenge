class Solution {
  public:
    int findMin(vector<int>& arr) {
        int min=arr[0],high=arr.size()-1;
        for(int i=1;i<arr.size();i++){
            if(arr[i]<min)
              min=arr[i];

        }
        return min;
    }
};
